<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialIcons="clr-namespace:Material.Icons.Avalonia;assembly=Material.Icons.Avalonia"
        xmlns:lvc="using:LiveChartsCore.SkiaSharpView.Avalonia"
        xmlns:skiaSharpView="clr-namespace:LiveChartsCore.SkiaSharpView;assembly=LiveChartsCore.SkiaSharpView"
        mc:Ignorable="d" d:DesignWidth="600" d:DesignHeight="900"
        Icon="avares://ObhodBApp/icon.ico"
        Height="900"
        Width="600"
        x:Class="ObhodBApp.MainWindow"
        Title="ObhodBApp">
    <DockPanel>
        <TabControl DockPanel.Dock="Top" Margin="0">
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" Spacing="5">
                        <materialIcons:MaterialIcon Kind="Home"/>
                    </StackPanel>
                </TabItem.Header>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="300"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DropDownButton Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Top"
                                    CornerRadius="100" FontSize="30" Padding="10"
                                    FontWeight="Bold" Margin="20"
                                    Content="Выберите профиль NGPN">
                        <DropDownButton.Flyout>
                            <MenuFlyout>
                                <MenuItem Header="Option 1" />
                                <MenuItem Header="Option 2" />
                                <MenuItem Header="Option 3" />
                            </MenuFlyout>
                        </DropDownButton.Flyout>
                    </DropDownButton>

                    <Button Grid.Row="1" Name="MainBtn" Margin="40" HorizontalAlignment="Center"
                            CornerRadius="100" Click="MainBtn_OnClick">
                        <materialIcons:MaterialIcon Name="MainBtnIcon" Padding="20" Kind="Web" IconSize="150" />
                    </Button>

                    <TextBlock Grid.Row="2" FontSize="20" FontWeight="Bold"
                               Name="IpInfo" Text="Ваш IP: ..."
                               HorizontalAlignment="Center"/>

                    <Button ToolTip.Placement="Bottom" Grid.Row="3" CornerRadius="100"
                            HorizontalAlignment="Center" Margin="10" Padding="10"
                            FontSize="20" FontWeight="Bold">
                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <materialIcons:MaterialIcon Kind="Upload" Foreground="Orange"/>
                            <TextBlock VerticalAlignment="Center" Name="UplText">10Mb/s</TextBlock>
                            <materialIcons:MaterialIcon Kind="Download" Foreground="Green"/>
                            <TextBlock VerticalAlignment="Center" Name="DwlText">10Mb/s</TextBlock>
                        </StackPanel>

                        <ToolTip.Tip>
                            <Border>
                                <Border.RenderTransform>
                                    <ScaleTransform ScaleX="1.5" ScaleY="1.5"/> <!-- масштаб 1.5x -->
                                </Border.RenderTransform>

                                <StackPanel Margin="5">
                                    <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                        <materialIcons:MaterialIcon Kind="ClockTimeEight" Foreground="Orange"/>
                                        <TextBlock VerticalAlignment="Center" Name="Time">Время работы...</TextBlock>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                        <materialIcons:MaterialIcon Kind="Upload" Foreground="Orange"/>
                                        <TextBlock VerticalAlignment="Center" Name="MUplText">Отправка</TextBlock>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                        <materialIcons:MaterialIcon Kind="Download" Foreground="Green"/>
                                        <TextBlock VerticalAlignment="Center" Name="MDwlText">Загрузка</TextBlock>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                        <materialIcons:MaterialIcon Kind="AlertCircle" Foreground="Red"/>
                                        <TextBlock VerticalAlignment="Center" Name="ErrPckText">Пакеты с ошибкой</TextBlock>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </ToolTip.Tip>
                    </Button>

                    <Button Grid.Row="4" CornerRadius="100" HorizontalAlignment="Center"
                            Margin="10" Padding="10" FontSize="20" FontWeight="Bold">
                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <materialIcons:MaterialIcon Kind="TrayDownload"/>
                            <TextBlock VerticalAlignment="Center" Name="TrafficText">10Mb/s</TextBlock>
                        </StackPanel>
                    </Button>

                    <lvc:CartesianChart Grid.Row="5" Name="Chart" Height="300">
                        <lvc:CartesianChart.XAxes>
                            <skiaSharpView:Axis IsVisible="False" />
                        </lvc:CartesianChart.XAxes>
                        <lvc:CartesianChart.YAxes>
                            <skiaSharpView:Axis IsVisible="True" />
                        </lvc:CartesianChart.YAxes>
                    </lvc:CartesianChart>
                </Grid>
            </TabItem>
            
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" Spacing="5">
                        <materialIcons:MaterialIcon Kind="Console"/>
                        <TextBlock Text="Консоль"  FontWeight="Bold"/>
                    </StackPanel>
                </TabItem.Header>

                <ScrollViewer VerticalScrollBarVisibility="Auto" CornerRadius="100"
                              HorizontalScrollBarVisibility="Auto">
                    <TextBlock Name="ConsoleTextBlock" Padding="20" FontSize="18"
                               FontFamily="Consolas" TextWrapping="Wrap" Background="Black"/>
                </ScrollViewer>
            </TabItem>

            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" Spacing="5">
                        <materialIcons:MaterialIcon Kind="Settings"/>
                        <TextBlock Text="Настройки"  FontWeight="Bold"/>
                    </StackPanel>
                </TabItem.Header>
                
                <ScrollViewer>
        <StackPanel Margin="20" Spacing="20" HorizontalAlignment="Left">
            <!-- 🔹 Группа 1: Общие настройки -->
            <StackPanel Spacing="10">
                <TextBlock Text="Общие настройки"
                           FontSize="24"
                           FontWeight="Bold"
                           Margin="0,0,0,10" />

                <!-- Текстовая настройка -->
                <Border Padding="10" CornerRadius="8">
                    <StackPanel Spacing="5">
                        <TextBlock Text="Имя пользователя" FontWeight="Bold" />
                        <TextBlock Text="Отображается в заголовке окна и других местах." FontSize="12" Foreground="Gray" />
                        <TextBox Width="250" />
                    </StackPanel>
                </Border>

                <!-- Числовая настройка -->
                <Border Padding="10" CornerRadius="8">
                    <StackPanel Spacing="5">
                        <TextBlock Text="Максимальное количество элементов" FontWeight="Bold" />
                        <TextBlock Text="Определяет, сколько записей будет загружено одновременно." FontSize="12" Foreground="Gray" />
                        <NumericUpDown Minimum="1" Maximum="1000" Width="150" />
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- 🔹 Группа 2: Внешний вид -->
            <StackPanel Spacing="10">
                <TextBlock Text="Внешний вид"
                           FontSize="24"
                           FontWeight="Bold"
                           Margin="0,10,0,10" />

                <!-- Переключатель -->
                <Border Padding="10" CornerRadius="8">
                    <StackPanel Spacing="5">
                        <TextBlock Text="Тёмная тема" FontWeight="Bold" />
                        <TextBlock Text="Включает или выключает тёмный режим интерфейса." FontSize="12" Foreground="Gray" />
                        <ToggleSwitch/>
                    </StackPanel>
                </Border>

                <!-- Выпадающий список -->
                <Border Padding="10" CornerRadius="8">
                    <StackPanel Spacing="5">
                        <TextBlock Text="Язык интерфейса" FontWeight="Bold" />
                        <TextBlock Text="Выберите язык приложения." FontSize="12" Foreground="Gray" />
                        <ComboBox Width="200">
                            <ComboBoxItem>Русский</ComboBoxItem>
                            <ComboBoxItem>English</ComboBoxItem>
                            <ComboBoxItem>Deutsch</ComboBoxItem>
                        </ComboBox>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Кнопка сохранения -->
            <Button Content="💾 Сохранить настройки"
                    Width="200"
                    HorizontalAlignment="Right"/>
        </StackPanel>
    </ScrollViewer>
            </TabItem>
            
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" Spacing="5">
                        <materialIcons:MaterialIcon Kind="Edit"/>
                        <TextBlock Text="Конфиг"  FontWeight="Bold"/>
                    </StackPanel>
                </TabItem.Header>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
